# Swifty Codegen

```bash
# Streaming Chat Model (Vanilla HTML or multiple files)
openai pull qwen3.5:cloud
# Route Chat Model
openai pull qwen2.5

openai serve

docker exec -i mysql mysql -uroot -ppass < ./sql/main.sql
```
